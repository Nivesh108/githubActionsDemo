name: daily-email

on:
    schedule:
        - cron: '*/5 * * * *'

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: first-step
              run: echo "Hello World" 